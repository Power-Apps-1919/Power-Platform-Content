---
title: "Time Picker"
description: "Time selection component with configurable boundaries and interval."
date: "2026-09-16"
tags:
  - components
  - input-utility
  - time
---

<div class="page-kicker">COMPONENT / INPUT UTILITY</div>

# Time Picker

Time selection component with configurable boundaries and interval. Download the original definition and use it with a component
library or another supported source-code workflow for your app.

<div class="component-actions">
  <a href="/Power-Platform-Content/downloads/components/cmp-time-picker.yml" download="cmp-time-picker.yml">Download YAML source</a>
  <span>Updated 16 Sep 2026</span>
</div>

## Component documentation

## Business value
Help users select a valid time consistently for appointments, service windows, shifts, bookings, or scheduled work.

## Best for
- Appointment and reservation forms.
- Operational schedules and service slots.
- Processes with start, end, and interval rules.

## What to provide
- `DefaultTime`: the initial value.
- `DependentControlTime`: a related time that affects availability.
- `EndTime`: the latest selectable time.
- `Interval`: the allowed increment.

## Getting started
Use `cmp-time-picker.yml` as a reference while creating or maintaining the equivalent component in a component library or approved source workflow; confirm the time zone and business hours used by your process, and validate the final value before saving it.

## Using the component YAML safely

This download is a complete `ComponentDefinitions` source file. It is useful as a
versioned reference and for supported source-code workflows, but it should not be
treated as an ordinary control snippet.

### Recommended reusable approach: component library

For a component that will be used across apps, Microsoft recommends a Power Apps
component library:

1. Open [Power Apps](https://make.powerapps.com).
2. Open **Component libraries** from **More** or **Discover all**, then create or
   open a library.
3. Create or maintain the component in the library and test it on a library screen.
4. Save the library, add version notes, and select **Publish**.
5. In the Canvas App, select **Insert** or **+**, choose **Get more components**,
   and select the shared library.
6. Add the component and configure its input, output, action, or event properties.

Published library versions can be updated and reviewed by apps that use them. This
is the best option when several makers or apps need the same component.

### Use a component in an individual app

For reuse only within one app, you can create an app-local component in Power Apps
Studio:

1. Open the app and select **Tree view**.
2. Select the **Components** tab, then **New component**.
3. Add the controls and custom properties that define the component.
4. On a screen, use **Insert** → **Components** to add an instance.

App-local components can be reused on multiple screens in that app. Changes remain
within that app; they do not provide the cross-app versioning and update
notifications of a component library.

### Copy and paste supported control YAML

Power Apps Studio also has **View code** for an individual control. Microsoft
documents this workflow for YAML generated by Studio:

1. Open the Canvas App for editing.
2. In the tree view or on the canvas, right-click an individual control and select
   **View code**.
3. Select **Copy code**, or use `Ctrl+C`.
4. In the destination app, select a screen or suitable location in the tree view.
5. Use **Paste** or `Ctrl+V` to create the control. This can be another screen in
   the same app or a different canvas app.
6. Approve browser clipboard access if Power Apps asks for it.

Studio validates the pasted code. This workflow applies to YAML that Power Apps
generated for a selected control and its child controls. It does not mean that a
full `ComponentDefinitions` file can be pasted into any code-view window.

### Can this complete file be pasted directly?

Do not assume that copying the entire downloaded file and pasting it into Code view
will create this component. These files define a Canvas component and its
properties, while Code view is documented for selected controls. If Studio rejects
the paste, that is expected for an unsupported format or location.

Use the component-library workflow for shared components. The older feature for
importing and exporting components between canvas apps is retired and disabled by
default; Microsoft documents a temporary per-app exception under **Settings** →
**Upcoming features** → **Retired** where that option is still available. Prefer
component libraries instead. If your organisation uses a supported source-code,
unpacking, or ALM process, keep this YAML in source control and follow that process
to apply it.

## Best practices

- Test first in a development environment with sample data.
- Keep the component interface small and use clear property names and descriptions.
- Pass business data through documented input properties rather than hard-coding records.
- Use output, action, and event properties only when the host app needs them.
- Confirm connector permissions, licensing, accessibility, responsive layout, and colour contrast.
- Save component-library versions with meaningful notes and publish changes deliberately.
- Validate formulas and data schemas after every update.
- Do not place secrets, connection credentials, personal data, or production exports in source files or downloads.

Microsoft references: [Canvas component overview](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/create-component), [Component library](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/component-library), and [Use code view](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/code-view).

## Source preview

```yaml
ComponentDefinitions:
  TimePicker:
    DefinitionType: CanvasComponent
    AccessAppScope: true
    CustomProperties:
      DefaultTime:
        PropertyKind: Input
        DisplayName: Default Time
        Description: The default time value shown when the picker loads.
        RaiseOnReset: true
        DataType: DateAndTime
        Default: =DateTimeValue(Time(10, 0, 0))
      DependentControlTime:
        PropertyKind: Input
        DisplayName: Dependent Control Time
        Description: The time value from a dependent control, used to adjust available time options.
        RaiseOnReset: true
        DataType: DateAndTime
        Default: =DateTimeValue(Time(9, 0, 0))
      EndTime:
        PropertyKind: Input
        DisplayName: End Time
        Description: The latest selectable time in the picker.
        RaiseOnReset: true
        DataType: DateAndTime
        Default: =DateTimeValue(Time(22, 0, 0))
      Interval:
        PropertyKind: Input
        DisplayName: Time Interval (Minutes)
        Description: The interval, in minutes, between selectable times.
        RaiseOnReset: true
        DataType: Number
        Default: =30
      OnChange:
        PropertyKind: Event
        DisplayName: OnChange
        Description: Triggered when the selected time changes.
        ReturnType: None
        Default: =
      Reset:
        PropertyKind: Action
        DisplayName: Reset
        Description: Resets the selected time to the default value.
        ReturnType: None
      SelectedTime:
        PropertyKind: Output
        DisplayName: Selected Time
        Description: The currently selected time value.
        DataType: DateAndTime
      StartTime:
        PropertyKind: Input
        DisplayName: Start Time
        Description: The earliest selectable time in the picker.
        RaiseOnReset: true
        DataType: DateAndTime
        Default: =DateTimeValue(Time(8, 0, 0))
    Properties:
      Height: =35
      Reset: =Select(ResetIcon)
      SelectedTime: =DateTimeValue(TimeComboBox.Selected.Value)
      Width: =139
    Children:
      - TimeComboBox:
          Control: ComboBox@0.0.51
          Properties:
            DefaultSelectedItems: |-
              =If(
                  Not IsBlankOrError(TimePicker.DefaultTime),
                  [
                      Text(
                          TimeValue(TimePicker.DefaultTime),
                          "[$-en-US]hh:mm AM/PM"
                      )
                  ].Value
              )
            Font: =Font.Arial
            FontColor: =RGBA(0, 0, 0, 1)
            FontSize: =14
            FontWeight: =FontWeight.Normal
            Height: =TimePicker.Height-5
            InputTextPlaceholder: ="Select time"
            Items: |
              =With(
                  {
                      varTotalIntervals: Round(
                          DateDiff(
                              If(
                                  IsBlankOrError(Parent.DependentControlTime),
                                  If(
                                      Or(
                                          Minute(Parent.StartTime) = 0,
                                          Minute(Parent.StartTime) = 30
                                      ),
                                      Parent.StartTime,
                                      If(
                                          Minute(Parent.StartTime) < 30,
                                          DateAdd(
                                              Parent.StartTime,
                                              30 - Minute(Parent.StartTime),
                                              TimeUnit.Minutes
                                          ),
                                          DateAdd(
                                              Parent.StartTime,
                                              60 - Minute(Parent.StartTime),
                                              TimeUnit.Minutes
                                          )
                                      )
                                  ),
                                  If(
                                      Or(
                                          Minute(Parent.DependentControlTime) = 0,
                                          Minute(Parent.DependentControlTime) = 30
                                      ),
                                      DateAdd(Parent.DependentControlTime, Parent.Interval, TimeUnit.Minutes),
                                      If(
                                          Minute(Parent.DependentControlTime) < 30,
                                          DateAdd(
                                              DateAdd(Parent.DependentControlTime, 30 - Minute(Parent.DependentControlTime), TimeUnit.Minutes),
                                              Parent.Interval,
                                              TimeUnit.Minutes
                                          ),
                                          DateAdd(
                                              DateAdd(Parent.DependentControlTime, 60 - Minute(Parent.DependentControlTime), TimeUnit.Minutes),
                                              Parent.Interval,
                                              TimeUnit.Minutes
                                          )
                                      )
                                  )
                              ),
                              Parent.EndTime,
                              TimeUnit.Minutes
                          ) / If(
                              IsNumeric(Parent.Interval),
                              Parent.Interval,
                              1
                          ),
                          0
                      )
                  },
                  ForAll(
                      Sequence(
                          IfError(
                              If(
                                  !IsBlankOrError(varTotalIntervals) && IsNumeric(varTotalIntervals),
                                  varTotalIntervals,
                                  1
                              ),
                              1
                          ) + 1,
                          0,
                          If(
                              IsNumeric(Parent.Interval),
                              Parent.Interval,
                              1
                          )
                      ),
                      {
                          Value: Text(
                              DateAdd(
                                  If(
                                      IsBlankOrError(Parent.DependentControlTime),
                                      If(
                                          Or(
                                              Minute(Parent.StartTime) = 0,
                                              Minute(Parent.StartTime) = 30
                                          ),
                                          Parent.StartTime,
                                          If(
                                              Minute(Parent.StartTime) < 30,
                                              DateAdd(
                                                  Parent.StartTime,
                                                  30 - Minute(Parent.StartTime),
                                                  TimeUnit.Minutes
                                              ),
                                              DateAdd(
                                                  Parent.StartTime,
                                                  60 - Minute(Parent.StartTime),
                                                  TimeUnit.Minutes
                                              )
                                          )
                                      ),
                                      If(
                                          Or(
                                              Minute(Parent.DependentControlTime) = 0,
                                              Minute(Parent.DependentControlTime) = 30
                                          ),
                                          DateAdd(Parent.DependentControlTime,0,  TimeUnit.Minutes),
                                          If(
                                              Minute(Parent.DependentControlTime) < 30,
                                              DateAdd(
                                                  DateAdd(Parent.DependentControlTime, 30 - Minute(Parent.DependentControlTime), TimeUnit.Minutes),
                                                  0,
                                                  TimeUnit.Minutes
                                              ),
                                              DateAdd(
                                                  DateAdd(Parent.DependentControlTime, 60 - Minute(Parent.DependentControlTime), TimeUnit.Minutes),
                                                  0,
                                                  TimeUnit.Minutes
                                              )
                                          )
                                      )
                                  ),
                                  ThisRecord.Value,
                                  TimeUnit.Minutes
                              ),
                              "[$-en-US]hh:mm AM/PM"
                          )
                      }
                  ).Value
              )
            OnChange: =Parent.OnChange()
            Width: =TimePicker.Width-5
            X: =2.5
            Y: =2.5
      - ResetIcon:
          Control: Classic/Icon@2.5.0
          Properties:
            Height: =33
            Icon: =Icon.Erase
            OnSelect: =Reset(TimeComboBox)
            Visible: =false
            Width: =31
            X: =106
```
